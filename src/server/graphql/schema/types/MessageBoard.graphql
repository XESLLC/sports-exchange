type TournamentMessage {
  id: ID!
  tournamentId: ID!
  entryId: ID
  entryName: String
  userId: ID!
  authorFirstName: String
  authorLastName: String
  title: String
  body: String!
  parentId: ID
  replies: [TournamentMessage]
  notifiedByEmail: Boolean!
  createdAt: String!
}

input CreateTournamentMessageInput {
  tournamentId: ID!
  title: String
  body: String!
  parentId: ID
}

type Query {
  # Returns top-level threads with nested replies. Accessible only to users
  # who have (or co-own) an entry in this tournament.
  tournamentMessages(tournamentId: ID!): [TournamentMessage]
}

type Mutation {
  # Accessible only to users who have (or co-own) an entry in this tournament.
  createTournamentMessage(input: CreateTournamentMessageInput!): TournamentMessage
}
