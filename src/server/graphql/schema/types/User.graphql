type User {
  id: ID!
  name: String
  stocks: [Int]
}

input IPOPurchaseInput {
  teamId: ID!
  tournamentId: ID!
  quantity: Int!
}

type Mutation {
  ipoPurchase(input: IPOPurchaseInput!): Boolean
}
